总结：
结论：自己的版本未能实现
1.尝试过将map key :时间戳，value：为价格，然后就将map遍历的方式，结果时间复杂度超过范围
2.问题点在，更新同一个时间戳，如何获取最大或者最小的股票价格
3.参考网上大神的写法，hashMap 来存放key：时间戳，value：价格，记录一个时间戳最大值，来获取最新价格，同时使用TreeMap
key：价格，value: 次数。更新方法：map包含时间戳，需要取出此次时间戳的价格，
用价格当做key从TreeMap取出次数，如果是第一次，直接从treeMap中去除掉，不为1，那么将treeMap中key:价格的次数减1。
4.新学方法：TreeMap的getOrDefault(),可以带默认值，获取不到就是默认的值，Math.max() 获取最大值方法的使用，不用写一堆if判断来那一个最大值的
5.耗时：大概5、6个小时吧
